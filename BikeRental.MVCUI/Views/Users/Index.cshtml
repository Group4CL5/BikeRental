@model IEnumerable<BikeRental.Models.Location>

@{
    ViewData["Title"] = "Index";
}

<h1>Store Locations</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.City)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id">View Available Bikes</a>               
            </td>
        </tr>
}
    </tbody>
</table>
